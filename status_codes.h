// The turn status of the player
typedef enum turn_status { inactive_turn, active_turn } t_status_t;

// The game status of the player
typedef enum game_status { inactive_game, active_game } g_status_t;

// ENUM IF PLAYER HOLDS THE FIRST TURN
typedef enum first_player { SP, FP } fPLayer_t;